{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h3>Fiche Évenement</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ evenement.titre }}</td>
            </tr>
            <tr>
                <th>Extrait</th>
                <td>{{ evenement.extrait }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ evenement.detail }}</td>
            </tr>
            <tr>
                <th>ImgUrl</th>
                <td>{{ evenement.imgUrl }}</td>
            </tr>
            <tr>
                <th>Epoque</th>
                <td>{{ evenement.epoque }}</td>
            </tr>
        </tbody>
    </table>

<button type="button" class="btn btn-outline-warning"><a href="{{ path('evenement_index') }}">Retour à la liste</a></button>

    <button type="button" class="btn btn-outline-warning"><a href="{{ path('evenement_edit', {'id': evenement.id}) }}">Modifier</a></button>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
