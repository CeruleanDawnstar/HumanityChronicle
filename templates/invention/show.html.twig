{% extends 'base.html.twig' %}

{% block title %}Fiche Invention{% endblock %}

{% block body %}
    <h3>Fiche Invention</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invention.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ invention.titre }}</td>
            </tr>
            <tr>
                <th>Extrait</th>
                <td>{{ invention.extrait }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ invention.detail }}</td>
            </tr>
            <tr>
                <th>ImgUrl</th>
                <td>{{ invention.imgUrl }}</td>
            </tr>
            <tr>
                <th>Epoque</th>
                <td>{{ invention.epoque }}</td>
            </tr>
        </tbody>
    </table>

<button type="button" class="btn btn-outline-warning"><a href="{{ path('invention_index') }}">Retour à la liste</a></button>

    <button type="button" class="btn btn-outline-warning"><a href="{{ path('invention_edit', {'id': invention.id}) }}">Modifier</a></button>

    {{ include('invention/_delete_form.html.twig') }}
{% endblock %}
